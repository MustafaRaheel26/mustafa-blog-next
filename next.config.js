/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['your-image-domain.com', 'example.com'], // Replace with actual image domains
    },
  };
  
  module.exports = nextConfig;
  